/*
 * DownLoader Messages
 *
 * This contains all the text for the DownLoader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DownLoader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DownLoader container!',
  },
});
