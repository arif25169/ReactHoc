/*
 * ImageLoader Messages
 *
 * This contains all the text for the ImageLoader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ImageLoader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageLoader container!',
  },
});
